# Handoff: MVI Migration (robustness pass)

**Status:** Phase 0 + Phase 1 complete; Phase 2 partially complete (4 chunks). Foundation + safety net are in place and proven. The remaining work is design-heavy and higher-risk (especially tab lifecycle = core session management).

**Goal (from the originating review):** stop whack-a-mole bugs by finishing the half-done CQRS/Elm-style MVI migration — collapse to a single webview→Command→reducer→(thin)Effect→runner path, make invariants machine-checkable, and hard-cut over per operation (delete legacy, no flags). Sub-agent-driven: scout for recon, worker for tight implementation, reviewer for verification, orchestrator commits.

**Decision recorded:** Option A (finish the MVI migration), hard cutover per op, delete legacy. Read `docs/ARCHITECTURE.md` and `docs/STATE_CONTRACT.md` first — they are authoritative for the invariants.

---

## What's done (5 commits, all reviewer-approved; `settings.json` is unrelated working-tree dirt — never commit it)

| Commit | Scope |
|--------|-------|
| `a60f31c` | **Phase 0.** Reducer/runner/command-handlers/result-handlers/streaming-handlers switches made compile-time total via `never` (a missing case is now a build error, not a silent log). 6 dead no-op effects (`FlashWindow`, `PlayCompletionSound`, `ExportRunAnalytics`, `EvictTranscript`, `DeriveFileChanges`, `DeriveAvailableExtensions`) + dead `ExportAnalyticsCommand` deleted. Fixed a real latent bug: `ExtensionUiResponseResult` was silently swallowed by *both* `default` branches — now routed. Purity boundary-guard tests added (ban `Date.now`/`Math.random`/`new Date()`/`console.*`/`process.env` and `vscode`/`node:fs`/`node:crypto` imports across the pure spine). Stale doc headers in `arch-state.ts`/`events.ts` fixed. |
| `a4b73a6` | **Phase 1.** `host/util/error-message.ts` `toErrorMessage(err)` normalizer + test. All 37 `(err as Error).message` sites → 0 (was yielding `"undefined"` for non-Error throws). De-silenced 2 lossy catches (`hydrateModelState`, `listAndOpenFirstSession`) — non-fatal, now logged. Intentional graceful-degradation catches left untouched. |
| `85b977c` | **Phase 2 — moveSessionTab.** Reducer owns the `openTabPaths` reorder (pure `moveOpenTabPath` from `shared/tab-behavior`) + emits `PersistTabs`. **Wired the `PersistTabs` Effect** (the `tabs.persistTabs` sink was a no-op stub — effects were silently dropped); now writes to globalState matching `SessionServiceState.saveOpenTabs()` exactly (shared `OPEN_TABS_STORAGE_KEY`/`ACTIVE_SESSION_STORAGE_KEY` exported from `state.ts`). Hard-deleted: `MoveSessionTabEffect`, `MoveSessionTabResult` event + reducer/result-handlers cases, `service.moveSessionTab`, `ReorderTabsCommand` + its reducer case. `assertSelectionInvariant` kept (other callers remain). |
| `415660d` | **Phase 2 — setPrefs.** Reducer owns the `unreadFinishedSessionPaths` clear (pure transition on `SetPrefs` when merged `suppressCompletionNotifications`). `service.setPrefs` no longer dispatches `UnreadFinishedSessionsChanged` (kept persistence + `runtimePrefs.set`). |
| `d0ced2c` | **Phase 2 — hydrateModelState.** `refreshState` dispatches a `HydrateModel` Command → reducer → fire-and-forget `HydrateModel` Effect → runner → `service.hydrateModelState` (impl unchanged). **The MessageRouter is now 100% Commands — zero SERVICE-DIRECT webview-message paths remain.** |

### Established, reusable patterns (follow these for every remaining migration)
1. **Reducer owns the state change + emits a thin Effect; the runner executes only the side effect.** This is the send/edit model (already in place for send/edit/interrupt/moveSessionTab/setPrefs).
2. **Hard cutover per op:** once the reducer owns it, delete the legacy Effect + Result event + reducer/result-handlers cases + service method + interface decls in the same chunk. The exhaustiveness `never` checks are the safety net — a dangling reference fails the build.
3. **Optimistic apply + revert-on-failure** (send/edit-style) for settings that can fail: snapshot the pre-change value in `PendingState` keyed by `corrId`; on `*Result{ok:false}` revert + notice; on `{ok:true}` clear the snapshot. (Used for send/edit; attempted for setPruningSettings — see design note.)
4. **Effects are thin descriptors**, not fat service calls. If an op needs a UI dialog (e.g. setModel's modal), model it as a dedicated `ShowModal` effect with a result event the reducer branches on — never do a modal inside an effect's service call.
5. **Persist via `PersistTabs` Effect** (now wired) rather than imperative `saveOpenTabs()` calls — migrate each tab op to emit `PersistTabs` and drop its `saveOpenTabs()` call.
6. **Sub-agent rhythm that works:** scout (recon, precise file:line map) → worker (TIGHT, one well-bounded change — workers time out on multi-change design tasks) → reviewer (independent verification) → orchestrator commits only the extension/ files (never `settings.json`). For design-heavy chunks, the orchestrator does the design + core implementation directly and uses workers only for mechanical sub-tasks.

### The safety net (keep it green; it catches real regressions)
- `extension/test/arch-boundary-guards.test.ts` — purity of the pure spine + import bans.
- The `never` exhaustiveness checks in `reducer.ts`, `effect-runner.ts`, `command-handlers.ts`, `result-handlers.ts`, `streaming-handlers.ts` — adding an Event/Effect/Command variant without a handler fails `tsc`.
- `npm run typecheck && npm run test && npm run build` after every `extension/src/` edit (per `AGENTS.md`). Suite is ~811 tests.

---

## Remaining work (in suggested risk order; file:line are hints and may have shifted — locate by symbol)

### Phase 2 — method orchestration-lifts (the meat; the router layer is DONE)

A foundational recon map already exists (a prior scout produced it; the key facts are below). **For every op, first re-confirm the current entry points with a quick grep** — the codebase is mid-migration and lines shift.

#### Lower-risk / minor
- **Delete dead `addComposerInput`/`removeComposerInput` service methods.** Verified unreachable from any runtime path: the router dispatches `AddComposerInput`/`RemoveComposerInput` Commands (reducer handles them — `command-handlers.ts`), and there is NO `AddComposerInput`/`RemoveComposerInput` Effect in `effects.ts`, so the `EffectRunner` never calls the service methods. Delete: `message-actions.ts` `addComposerInput`+`removeComposerInput`, `service.ts` facade delegations, and the vestigial interface decls in `message-router.ts` (the `SessionServiceLike` it holds). Grep `addComposerInput|removeComposerInput` across `src/`+`test/` first to confirm no caller.
- **`interrupt` suppress-flag (minor).** `onInterrupt` in `message-router.ts` still side-calls `this.service.suppressNextCompletionNotificationFor(sessionPath)` (the one pre-existing non-Command router side-call flagged by the reviewer). The flag (`SessionServiceState.suppressNextCompletionNotification: Set<string>`, `state.ts`) is host-side plumbing tied to the completion-notification side effect; its consume is an atomic read+delete (`consumeCompletionSuppression`) in a busy-completed handler — **moving it to pure reducer state reintroduces a read-vs-clear ordering hazard.** The clean, low-risk fix: move the *set* from the router side-call into the `EffectRunner`'s `InterruptRpc` path (the runner is the side-effect executor; setting a host-side flag there is appropriate). Keep the flag host-local. Do NOT try to make the reducer own this flag.

#### Design-thorny (needs a deliberate decision)
- **`setPruningSettings` — failure semantics.** A prior worker attempt was REVERTED (it added optimistic-apply + a `pruningSettingsSnapshots` PendingState field + a `mergePruningSettings` helper, but stalled before the revert path, leaving an inconsistent state). The blocker: `writePruningSettings` (`pruning-settings.ts`) **throws when `PI_CODING_AGENT_DIR` is absent**, and the existing `savePruningSettings` (`pruning-settings-persistence.ts`) **intentionally keeps+mirrors on disk failure** (graceful degradation: in-memory merge applied + mirrored to globalState + `onError` notice, no throw). `PI_CODING_AGENT_DIR` is *read* by the extension but *never set in `src/`* — it comes from the user's env, so the no-dir case is real. Decision needed before migrating:
  - **Option A (revert-on-failure):** reducer applies optimistically on `SetPruningSettings` + snapshots; `savePruningSettings` re-throws on failure (drop `dispatch`/`onError`/`getCurrent`); `SetPruningSettingsResult{ok:false}` reverts + notices. Cleanest model BUT regresses the no-dir graceful-degradation (users without `PI_CODING_AGENT_DIR` can't persist pruning settings at all — they'd revert). Only acceptable if the no-dir case is confirmed unreachable in production.
  - **Option B (preserve keep-on-failure):** reducer applies optimistically (instant UI); service keeps the catch+mirror+notice (no throw); `SetPruningSettingsResult` always `{ok:true}` (clears a now-dead snapshot). Leaves the redundant `PruningSettingsChanged` dispatch (idempotent) OR removes it for the SET path only (keep it for `loadPersistedPruningSettings`). The snapshot/revert becomes dead — drop it. Modest win (instant UI + thinner service), no behavior regression.
  - **Recommendation:** confirm whether `PI_CODING_AGENT_DIR` is always set in production first (trace how the backend is spawned / whether the extension sets it from `globalStorageUri`). If always set → Option A; if not → Option B. The pure `mergePruningSettings(current, updates)` helper (top-level scalars replaced; `skillAlwaysKeep`/`toolAlwaysKeep` arrays replaced+copied) is the right merge for the reducer regardless — re-add it to `shared/protocol/settings.ts`. Note the disk write (`writePruningSettings`) has its OWN merge reading `settings.json`; the reducer's `mergePruningSettings` must produce the same shape (it does) so optimistic state == disk state.
- **Transcript paging (`loadOlder`/`loadNewer`/`jumpToLatest`).** `message-actions.ts` `loadTranscriptPage` (private; wrappers `loadOlder/Newer/jumpToLatestTranscript`) holds host-local state: `inFlightTranscriptPageBySession` set + `transcriptPageRequestSeqBySession` counter + `getSessionDataEpoch` staleness checks (3 re-checks after the RPC) + `evictInactiveTranscriptWindows` (host-side LRU dispatching `TranscriptTrimmed`). The reducer owns none of this. Design: the in-flight guard + seq counter could become reducer/Effect bookkeeping; the epoch staleness check is host-local and may stay; eviction is host-side LRU policy (could become reducer policy in Phase 3/4). This is a medium chunk — scope it as one op, TDD the reducer-owned parts, keep host-side staleness/eviction for now.

#### High-value, high-risk (core functionality — extra care + per-op TDD)
- **`setModel` — modal as effect.** `service.setModel` (`message-actions.ts`) does: a `vscode.window.showWarningMessage({modal:true})` to confirm clearing pending images, `enqueueLifecycle`+`backend.request('settings.get')`-style `settings.set`, `dispatchArch ContextUsageChanged(null)`, `bumpSessionDataEpoch`, `dispatchArch SessionMetadataChanged`, `clearPendingImageInputs`, `runObserver.onModelConfigChanged`, `scheduleRender`, `catch`→`Error`. **Split-brain:** the `SetModel` Command reducer already sets `settings.modelSettings` optimistically AND the service re-dispatches `SessionMetadataChanged`. Migration: (a) the modal prompt → a new `ShowModal` Effect carrying the question + a `ModalResponse` result event; the reducer branches on the response (proceed/abort). (b) `ContextUsageChanged(null)` + `SessionMetadataChanged` → `SetModelResult`-driven reducer transitions. (c) `clearPendingImageInputs` → reducer transition on the `SetModel` Command (when the new model lacks image support). (d) `bumpSessionDataEpoch` + `runObserver.onModelConfigChanged` → keep as Effect-side concerns (epoch is host-local; observer may subscribe to ArchState projections in Phase 4). This is the canonical "effects are thin, the reducer is the brain" demonstration.
- **Tab lifecycle — `createSession`/`openSession`/`closeSession`/`duplicateSession`** (`session-service/tab-actions.ts`). **The bulk of the remaining split-brain and the most bug-prone; core session management.** Each: allocates a pending path (`state.createPendingSessionPath`), registers a selection token + 60s timer (`state.beginSelectionRequest`), dispatches `SessionSummaryUpserted` (placeholder) + `TabOpened` + a `SelectSession` **Command** (the service re-enters the Command spine to mutate active session — this is the core smell) + `RunningSessionsChanged` + `ActiveRunSummaryChanged(null)`, calls `saveOpenTabs()` (→ now emit `PersistTabs` instead), `enqueueLifecycle(backend.request('session.create'|'session.open'|'session.duplicate'))` with `handleSelectionFailure` (which dispatches `CloseTab`/`SelectSession`/`NoticeShown` + `clearSessionScope`). **`closeSession` recursively calls `openSession(nextPath)`** when closing the active tab (the biggest orchestration hazard — a single Effect can't model the recursive open). `openSession` is also called from `closeSession`, `publishBackendReady` (`backend-ready.ts`), and `listAndOpenFirstSession` (`startup.ts`) — so it has multiple entry points.
  - **Migration approach (per op, hard cutover, TDD):** move the placeholder-summary + tab-open + select + running-state transitions into the reducer's Command handler (pure), emit the backend-RPC lifecycle Effect (thin) + `PersistTabs`. The selection-token + timer is host-local lifecycle bookkeeping — keep it host-side (the runner owns it) OR move to a dedicated `BeginSelectionRequest` Effect. The recursive `closeSession`→`openSession` is the hard part: model close as a reducer transition that, when closing the active tab, emits *both* a `CloseSession` RPC Effect and an `OpenSession` lifecycle Effect for the next path (the reducer decides the next tab, not the service). `handleSelectionFailure`'s `CloseTab`/`SelectSession`/`NoticeShown` dispatches should become reducer transitions driven by the lifecycle `*Result{ok:false}`. Do these ONE AT A TIME (createSession first, then openSession, then duplicateSession, then closeSession last) with full TDD + manual smoke-test of create/open/close/duplicate, because a regression here breaks core functionality.
- **`addFilesystemPaths`** (`message-actions.ts`): `resolveComposerTargetSessionPath` may call `createNewSession()` as a fallback (a file-picker attach can spawn a new session) — the most entangled method. Defer until tab lifecycle is migrated (it depends on `createNewSession` being a clean Command/Effect).

### Phase 3 — collapse `QueueManager` into `ArchState`
`core/queue-manager.ts` holds `pendingSendQueue: Map` + `backendReadyQueue: array` + a 30s watchdog timer — state OUTSIDE `ArchState`. It dispatches `OptimisticMessageInserted`/`SessionNameDerived` directly and re-dispatches sends via `handleMessage` on drain. Move these queues into `ArchState` as `Record`s (Record-only rule); the watchdog timeout becomes a timer Effect → `NoticeShown`; drain-on-`BackendReadyChanged` becomes a reducer reaction. This removes the last large chunk of non-ArchState state. (Use `Record<string,T>`, not `Map`, per the contract.)

### Phase 4 — delete legacy `SessionService` orchestration
Once every op routes through Command→reducer→effect with the reducer owning transitions, strip the orchestration from `SessionService`; it becomes a thin backend adapter + lifecycle (backend client start/stop, session boot). Shrink `SessionServiceState` (`state.ts`, ~461 lines — holds epoch counters, selection requests, transcript-page in-flight, transcript-window LRU, suppress flags, busy seq). `runObserver` calls sprinkled across `handlers/*` + `message-actions.ts` + `tab-actions.ts` — decide whether the observer subscribes to ArchState projections or stays as Effect-side calls. The module-global `subscribeToArchState` auto-projection (`dispatch.ts` + `extension-host.ts`) can become an explicit `selectViewState` call after dispatch.

### Phase 5 — harden recovery + webview
- **Optimistic-op TTL:** if no `*Result` arrives within N seconds, reconcile against backend (query session) or revert + notify. Today optimistic messages + `pending.ops[corrId]` live forever if a result is lost (host crash mid-send). No escape hatch.
- **Transport recovery:** `sidebar/sync.ts` `reconcilePostedMessageDelivery` only marks `globalDirty` for `type==='state'` — a lost `sendRejected` (restores the user's draft) or imperative is silently dropped (data loss). Recover all state-bearing host→webview messages. `canPostSnapshotToWebview` ignores the `webviewReady` flag even though the provider tracks it — gate posting on ready. The webview message listener (`webview/panel/hooks/use-host-sync.ts`) doesn't verify `event.source`/origin.
- **Recovery-by-reload:** a missed `stateApplied` ack force-reloads the whole webview (`provider.ts`: `STATE_APPLIED_TIMEOUT_MS=2500`, `STATE_APPLIED_RELOAD_LIMIT=2`/30s) — a sledgehammer that can cause reload storms on slow transcripts. Replace with re-snapshot-first; reload only after consecutive unacked re-snapshots.
- **Webview state-ownership leaks (contract violations):** module-level `Map` disclosure cache (`webview/panel/transcript/use-disclosure-open.ts`) shadowing host `autoExpand*` prefs and never cleared on host-instance/session change (clear violation); local `dismissed` boolean for host-owned error content (`transcript/message-item/error-detail.tsx`); ~14 borderline disclosure locals; fragile composer draft re-sync keyed only on `[sessionPath]` (`composer/hooks.ts`); `pendingDraftRestoreRef` Map never cleared on session close (`use-host-sync.ts` `clearTransientUi`). Move disclosure/error-dismissal into host state (or at minimum clear the module Map on host-instance/session change); re-sync composer draft on host `draftText` change.

---

## How to continue (concrete first steps for the next session)
1. `cd extension && npm run typecheck && npm run test && npm run build` — confirm the foundation is green (expect ~811 tests, ~810 pass, 1 pre-existing skip).
2. Pick the next chunk from the risk-ordered list above. For the lower-risk ones, dispatch a TIGHT worker (one well-bounded change). For design-thorny/high-risk ones, the orchestrator designs + implements the core directly, uses workers for mechanical sub-tasks, and uses the reviewer gate.
3. Re-confirm current entry points with a grep before each migration (lines shift mid-migration).
4. Commit only `extension/` files; **never** `settings.json` (unrelated model-pref working-tree dirt).
5. Every chunk: TDD (write/extend a reducer/integration test capturing behavior) → migrate → `never`-checks still compile → typecheck+test+build green → reviewer → commit.

## Key file map (symbols to locate, not line numbers)
- Pure spine: `host/core/reducer.ts`, `arch-state.ts`, `effects.ts`, `events.ts`, `commands.ts`, `projection.ts`, `host/core/reducer/*`.
- Impure plumbing in `core/` (NOT pure — excluded from purity guards): `host/core/message-router.ts`, `queue-manager.ts`, `effect-runner.ts`, `dispatch.ts`, `event-dispatch.ts`, `file-diff-service.ts`.
- Service orchestration (Phase 2 targets): `host/session-service/{service.ts, state.ts, tab-actions.ts, message-actions.ts, startup.ts, handlers/*, pruning-settings.ts, pruning-settings-persistence.ts}`.
- Transport: `host/sidebar/{sync.ts, provider.ts, completion-notification.ts}`; webview: `webview/panel/hooks/use-host-sync.ts` + the components listed in Phase 5.
- Normalizer + guards: `host/util/error-message.ts`, `test/arch-boundary-guards.test.ts`.
- PersistTabs wiring + storage keys: `host/extension-host.ts` (`tabs.persistTabs`), `host/session-service/state.ts` (`OPEN_TABS_STORAGE_KEY`/`ACTIVE_SESSION_STORAGE_KEY`, `saveOpenTabs`).