# Handoff: MVI Migration (robustness pass)

**Status:** Phase 0 + Phase 1 complete; **Phase 2 COMPLETE (13 of its chunks done — see table).** **Phase 3 COMPLETE (2 chunks — `QueueManager` fully deleted, both queues collapsed into `ArchState`).** **Phase 4 COMPLETE (2 chunks — mechanical cleanup + `saveOpenTabs` removal; runObserver decision recorded below).** **Phase 5 COMPLETE (12 items done — see table below).** Foundation + safety net are in place and proven. **All method-orchestration-lifts are DONE: tab lifecycle (create/open/duplicate/close) + setModel + transcript paging + addFilesystemPaths.** The MessageRouter is 100% Commands. **The migration is complete — all deferred items resolved.** (createSession is done — `9343b62`; openSession is done — `36983fc`; duplicateSession is done — `882fe34`; closeSession is done — `d9712c7`; setModel is done — `6b84d2a`, modal-as-effect; transcript paging is done — `7ffec0d`; addFilesystemPaths is done — `ac4fa86`; pendingSendQueue is done — `2c18069`; backendReadyQueue + watchdog is done — `7605534`.)

**Goal (from the originating review):** stop whack-a-mole bugs by finishing the half-done CQRS/Elm-style MVI migration — collapse to a single webview→Command→reducer→(thin)Effect→runner path, make invariants machine-checkable, and hard-cut over per operation (delete legacy, no flags). Sub-agent-driven: scout for recon, worker for tight implementation, reviewer for verification, orchestrator commits.

**Decision recorded:** Option A (finish the MVI migration), hard cutover per op, delete legacy. Read `docs/ARCHITECTURE.md` and `docs/STATE_CONTRACT.md` first — they are authoritative for the invariants.

---

## What's done (22 commits, all reviewer-approved; `settings.json` is unrelated working-tree dirt — never commit it)

| Commit | Scope |
|--------|-------|
| `a60f31c` | **Phase 0.** Reducer/runner/command-handlers/result-handlers/streaming-handlers switches made compile-time total via `never` (a missing case is now a build error, not a silent log). 6 dead no-op effects (`FlashWindow`, `PlayCompletionSound`, `ExportRunAnalytics`, `EvictTranscript`, `DeriveFileChanges`, `DeriveAvailableExtensions`) + dead `ExportAnalyticsCommand` deleted. Fixed a real latent bug: `ExtensionUiResponseResult` was silently swallowed by *both* `default` branches — now routed. Purity boundary-guard tests added (ban `Date.now`/`Math.random`/`new Date()`/`console.*`/`process.env` and `vscode`/`node:fs`/`node:crypto` imports across the pure spine). Stale doc headers in `arch-state.ts`/`events.ts` fixed. |
| `a4b73a6` | **Phase 1.** `host/util/error-message.ts` `toErrorMessage(err)` normalizer + test. All 37 `(err as Error).message` sites → 0 (was yielding `"undefined"` for non-Error throws). De-silenced 2 lossy catches (`hydrateModelState`, `listAndOpenFirstSession`) — non-fatal, now logged. Intentional graceful-degradation catches left untouched. |
| `85b977c` | **Phase 2 — moveSessionTab.** Reducer owns the `openTabPaths` reorder (pure `moveOpenTabPath` from `shared/tab-behavior`) + emits `PersistTabs`. **Wired the `PersistTabs` Effect** (the `tabs.persistTabs` sink was a no-op stub — effects were silently dropped); now writes to globalState matching `SessionServiceState.saveOpenTabs()` exactly (shared `OPEN_TABS_STORAGE_KEY`/`ACTIVE_SESSION_STORAGE_KEY` exported from `state.ts`). Hard-deleted: `MoveSessionTabEffect`, `MoveSessionTabResult` event + reducer/result-handlers cases, `service.moveSessionTab`, `ReorderTabsCommand` + its reducer case. `assertSelectionInvariant` kept (other callers remain). |
| `415660d` | **Phase 2 — setPrefs.** Reducer owns the `unreadFinishedSessionPaths` clear (pure transition on `SetPrefs` when merged `suppressCompletionNotifications`). `service.setPrefs` no longer dispatches `UnreadFinishedSessionsChanged` (kept persistence + `runtimePrefs.set`). |
| `d0ced2c` | **Phase 2 — hydrateModelState.** `refreshState` dispatches a `HydrateModel` Command → reducer → fire-and-forget `HydrateModel` Effect → runner → `service.hydrateModelState` (impl unchanged). **The MessageRouter is now 100% Commands — zero SERVICE-DIRECT webview-message paths remain.** |
| `487b430` | **Phase 2 — composerInputs (dead-code cutover).** Hard-deleted the dead `addComposerInput`/`removeComposerInput` path: the router already dispatched `AddComposerInput`/`RemoveComposerInput` Commands to the reducer (which owns the `pendingComposerInputsBySession` change, `effects: []`), so the `SessionService` methods + `service.ts` facade + `message-router.ts` `SessionServiceLike` decls were unreachable. Deleted all three + the now-unused `ComposerInputDraft` imports. 3 characterization tests for the live reducer path. |
| `66f9479` | **Phase 2 — interrupt suppress-flag.** Moved the `suppressNextCompletionNotificationFor` SET out of `onInterrupt`'s non-Command router side-call into the `EffectRunner`'s `InterruptRpc` path (set synchronously in `runRpc` before the async enqueue — same-tick eager-set preserved via `dispatchArchEvent`'s synchronous effect loop). Added the decl to the runner's `SessionServiceLike`; removed it from the router's. Flag stays host-local (no reducer ownership — avoids the read-vs-clear ordering hazard). 2 TDD tests. |
| `fca20c7` | **Phase 2 — setPruningSettings (Option B).** Investigated `PI_CODING_AGENT_DIR` — only ever READ in `src/` (never set; backend spawned with `env: process.env`), so the no-dir case is real → Option B (not A). Reducer applies `mergePruningSettings` optimistically (instant UI) + emits the thin `SetPruningSettings` Effect; service keeps catch+mirror+notice (graceful degradation, no throw). Cleaner variant: the SET path no longer re-dispatches `PruningSettingsChanged` (avoids a lost-update UI flicker under rapid sequential changes); load path keeps its dispatch. Also fixed a pre-existing `writePruningSettings` gap — `prepassTimeoutSec` was read but never written (optimistic≠persisted + data loss across restart) — so the invariant holds for all 9 fields. 8 TDD tests. |
| `7ffec0d` | **Phase 2 — transcriptPaging (loadOlder/Newer/jumpToLatest).** Moved the in-flight guard + request-identity bookkeeping from the host-side `inFlightTranscriptPageBySession` Set + `transcriptPageRequestSeqBySession` Map on `SessionMessageActions` into reducer-owned `TranscriptState.pagingInFlightBySession: Record<string,string>` (keyed by the Command `corrId` — replaces the per-session seq counter; consistent with send/edit `PendingOp` correlation). Command handler guards (one in-flight paging per session) + sets the flag; `*Result` clears it only when `corrId` matches (a stale result from a superseded request — tab closed+reopened — does NOT clear the current flag; logs on `!ok` regardless); `handleSessionScopeCleared` clears it on tab close (a recovery path the old `finally` lacked). Epoch/window/open-tabs staleness checks + LRU eviction stay host-side (Phase 3/4 folds the reducer-state reads in). Added `sessionPath` to the 3 paging result events so the reducer can look up the flag; runner passes `effect.sessionPath`. `dispatchArchEvent` is synchronous → flag clears synchronously via the Result event (no timing regression vs the old `finally`). Hard-deleted: the Set, the Map, `nextTranscriptPageRequestSeq`, `isCurrentTranscriptPageRequest`, `dropSessionLocalState` (impl + service facade + router/queue-manager interface decls + queueDeps wiring + purge call) — 0 refs remain. 10 reducer tests (replaced 3 "state unchanged" tests). Reviewer PASS. |
| `6b84d2a` | **Phase 2 — setModel (modal as effect).** The reducer owns every ArchState transition; the runner is a thin executor. New `ShowModelSwitchConfirm` Effect + `ModelSwitchConfirmResult` result event (handoff pattern #4, now established). When a switch would drop pending pasted images — a pure `shouldClearPendingImages` read of ArchState via a new pure-spine `core/model-capability.ts` (`modelSupportsInputKind` extracted from the vscode-importing `composer.ts` so the reducer can use it without pulling vscode into the test graph; `composer.ts` re-exports for existing importers) — the reducer stashes a `pending.setModelByCorrId[corrId]` intent (`snapshot:null`) and emits the modal Effect with NO state change; `{confirmed}` applies optimistically (clearing images) + emits `SetModelRpc`, `{!confirmed}` drops the intent unchanged. The open/pending guard relocates from `service.requireOpenSessionPath` into the reducer (invalid session sets a notice and changes nothing). Optimistic apply = global `modelSettings` + per-session summary modelId/thinkingLevel + `contextUsageBySession[sp]=null` + pending-image clear, with a field-for-field `SetModelSnapshot` rollback keyed by `corrId`; `SetModelResult{ok:false}` reverts + notices, `{ok:true}` drops. `bumpSessionDataEpoch` + `runObserver.onModelConfigChanged` stay Effect-side (host-local epoch + disk-persisting analytics) via a new `service.onModelConfigChanged` delegate (`runObserver` stored as a `SessionService` field); the modal is intentionally NOT queued (VS Code serializes modals; the backend write stays queued — avoids blocking create/open behind the modal the way the old `enqueueLifecycle(modal)` did). Hard-deleted: `messages.setModel`, `service.setModel` wrapper, `clearPendingImageInputs` (only setModel used it), `setModel` decls in both `SessionServiceLike` interfaces (router + runner). `SessionScopeCleared` drops in-flight setModel entries (mirrors pattern #8). **Fixes two live bugs:** the double-execution/Cancel hazard (the reducer applied optimistically before the modal, so Cancel left the new model un-reverted) and the missing rollback (`SetModelResult` was a no-op). 22 TDD tests (14 reducer + 4 runner + 4 review edge-cases). Reviewer PASS (medium bucket; first pass caught a real data-loss regression — `revertSetModel` deleted present file-ref inputs on the no-modal path — fixed by snapshotting `previousPendingInputs` unconditionally; re-review PASS). |
| `9343b62` | **Phase 2 — createSession (tab lifecycle op 1 of 4).** The reducer owns the optimistic tab setup; the runner owns the backend `session.create` RPC + failure recovery. `service.createNewSession()` (host-side) generates the impure pending path + placeholder summary, mints the selection token via `beginSelectionRequest` **before** dispatching the `CreateSession` Command (so it snapshots the previous active path for failure recovery), and returns the pending path synchronously (composer fallback still works — `dispatchArch` is synchronous so the reducer setup is applied before return). The reducer's `CreateSession` handler inserts the placeholder summary, opens+selects the tab, ensures it's not running, clears the active-run summary, and emits `PersistTabs` + `CreateSession` Effect; the runner issues `backend.request('session.create', {cwd, selectionToken})` on the lifecycle queue and calls `service.handleSelectionFailure(token)` on catch (which dispatches the reducer transitions that undo the optimistic setup — `SessionScopeCleared` + `SelectSession`-fallback + `NoticeShown` — so `CreateSessionResult` stays a no-op, matching the pre-migration recovery path). `CreateSessionCommand`/`Effect` drop the dead router-minted `selectionToken` UUID (the service ignored it; it minted its own) and now carry `sessionPath`/`cwd`/`placeholderSummary` + the real `beginSelectionRequest` token — unifying the two divergent token schemes the scout flagged. `message-router.onNewSession` calls `service.createNewSession()` (making the previously-vestigial router `service` dep actually used). Hard-deleted: the optimistic dispatches + `beginSelectionRequest` + the backend call + `handleSelectionFailure` catch + `saveOpenTabs()` from `tab-actions.createNewSession`; `createNewSession()` dropped from the runner's `SessionServiceLike` (router + composer fallback still call it); `handleSelectionFailure` added to it. The selection token + 60s timer + `handleSelectionFailure` stay host-side in `SessionServiceState` (unchanged) — the riskiest machinery is deliberately not moved in this chunk. **Key ordering invariant (reusable for openSession):** `beginSelectionRequest` MUST run before the reducer's optimistic `activeSessionPath` change, else its `previousActivePath` snapshot (used to restore the previously-active tab on failure) captures the pending path — a recovery bug caught in self-review and pinned by `create-session-ordering.test.ts`. `openSession`/`duplicateSession`/`closeSession` untouched (next chunks). 7 TDD tests (4 reducer + 1 ordering + 1 e2e recovery through the EffectRunner + 2 runner). Reviewer PASS (medium; the first long review-task attempt timed out at 600s — a tighter re-review PASSed, confirming the handoff's “use a tighter task” note). |
| `36983fc` | **Phase 2 — openSession (tab lifecycle op 2 of 4).** createSession (`9343b62`) is the template: the reducer owns the optimistic tab setup; the runner owns the backend `session.open` RPC + `handleSelectionFailure` recovery; the selection token is minted in `service.openSession()` BEFORE the reducer activates the opened tab. `OpenSessionCommand` gained `placeholderSummary` (a nullable `SessionSummary`, built host-side iff `!existing` since the impure `Date.now` can't live in the reducer); the reducer inserts it iff `!alreadySummarized`, appends to `openTabPaths` iff `!wasOpenTab`, sets `activeSessionPath`, clears `unreadFinishedSessionPaths` (SelectSession semantics), and emits `PersistTabs` + `OpenSession`. **Key semantic difference from CreateSession:** OpenSession deliberately does NOT touch `runningSessionPaths` or `activeRunSummaryBySession` — the opened session may be running, and stopping it or dropping its in-progress summary on a mere tab switch would be a regression (CreateSession clears both because a brand-new session cannot be running). `message-router.onOpenSession` consolidated to call `service.openSession()` (was dispatching the Command directly with a fake `crypto.randomUUID()` token never registered with `beginSelectionRequest` — so `handleSelectionFailure` could not restore the previous active tab; mirrors `onNewSession` → `createNewSession`). `handleSelectionFailure` stays host-side (unchanged); its `!wasOpenTab` branch (CloseTab + clearSessionScope + SelectSession-fallback + NoticeShown) undoes the optimistic setup, so `OpenSessionResult` stays a no-op. Hard-deleted: the imperative `SessionSummaryUpserted`/`TabOpened`/`SelectSession` dispatches + `saveOpenTabs` + `enqueueLifecycle(backend.request('session.open'))` + `handleSelectionFailure` catch from `tab-actions.openSession`; the runner's OpenSession stub (`service.openSession` call); `openSession` from the runner's `SessionServiceLike`. The recursive `closeSession`→`openSession(nextPath)` path still works (nextPath is already summarized+open by then, so the reducer no-ops placeholder/tab and just re-selects + emits the RPC; `handleSelectionFailure`'s `wasOpenTab=true` branch skips teardown on failure). 9 TDD tests (5 reducer + 2 ordering/e2e + 2 runner); 2 existing tests updated to wire a real `EffectRunner` (serialization) + assert the new runner behavior. Reviewer PASS (medium bucket, focused 7-risk task). |
| `882fe34` | **Phase 2 — duplicateSession (tab lifecycle op 3 of 4).** createSession (`9343b62`) + openSession (`36983fc`) are the template: the reducer owns the optimistic copy-tab setup; the runner owns the backend `session.duplicate` RPC + `handleSelectionFailure` recovery; the selection token is minted in `service.duplicateSession()` BEFORE the reducer activates the copy tab (pattern #9). `DuplicateSessionCommand` gained `sessionPath` (pending copy path) + `sourceSessionPath` + `placeholderSummary` + `selectionToken`; the reducer unshifts the placeholder copy summary (`name: "${source.name} (copy)"`, `messageCount` from source), opens the copy tab **ADJACENT to the source** (`insertAfter` semantics — matches `handleTabOpened`, the one UX difference from createSession which appends at end), selects it, ensures it's not running, and clears its active-run summary (**mirror createSession, NOT openSession** — a brand-new copy cannot be running), then emits `PersistTabs` + `DuplicateSession`. `message-router.onDuplicateSession` consolidated to call `service.duplicateSession()` (was dispatching a stub Command with only the source path — no pending path/placeholder/registered token — so the runner fell back to the old fat imperative path). `handleSelectionFailure` stays host-side (unchanged); its `pendingPath` branch (`SessionScopeCleared` + `SelectSession`-fallback + `NoticeShown`) undoes the optimistic setup, so `DuplicateSessionResult` stays a no-op (moved out of the generic Log-on-!ok fallthrough to a dedicated no-op handler, mirroring `CreateSessionResult`). `DuplicateSessionEffect` added to `isLifecycleEffect` so it serializes on the lifecycle queue with create/open via `runLifecycle`. Hard-deleted: the fat `tabs.duplicateSession` body (imperative dispatches + `enqueueLifecycle` + `handleSelectionFailure` catch), the old inline runner stub that called `service.duplicateSession()`, `duplicateSession` from the runner's `SessionServiceLike`, and the now-unused `backend` field/import + `toErrorMessage` + `SessionOpenedPayload` imports from `tab-actions` (duplicateSession was the last `SessionTabActions` method using `this.backend`; closeSession doesn't). 10 TDD tests (6 reducer + 2 ordering/e2e + 2 runner). Reviewer PASS (medium bucket; first pass approved + flagged a coverage gap — no params-asserting `DuplicateSession` runner test, so a source-vs-pending payload mixup could slip through silently — fixed by adding 2 runner tests asserting the `session.duplicate` payload carries the SOURCE path + the result echoes the pending copy path). |
| `d9712c7` | **Phase 2 — closeSession (tab lifecycle op 4 of 4 — the LAST).** createSession/openSession/duplicateSession are the template, with a key difference: there is NO backend RPC for close — the `CloseSession` Effect is a host-side cleanup descriptor, not a backend-RPC descriptor (NOT added to `isLifecycleEffect`; stays in the main `run()` body). The reducer's `CloseSession` handler was REWRITTEN from `removeSessionFromState` (full eviction: removed the summary, `runningSessionPaths`, nulled `activeSessionPath`) to the correct close semantics: computes `nextPath` via pure `getNextVisibleTabPathOnClose` FIRST (from the pre-close state), calls `handleSessionScopeCleared{removeSessionSummary:false}` to clear per-session keyed maps (KEEPS the summary for reopening, does NOT touch `runningSessionPaths` — the session may still be running in the backend even if its tab is closed), removes from `openTabPaths` + `unreadFinishedSessionPaths`, sets `activeSessionPath = nextPath ?? null` if the closed session was active, emits `PersistTabs` + `CloseSession` Effect (carrying `nextPath`). **This FIXES a latent double-execution bug:** the old handler nulled `activeSessionPath` BEFORE the runner's fat `service.closeSession()` could read it to select the next tab — so the next-tab selection was silently skipped. The runner's thin `service.closeSession(path, nextPath)` does ONLY the host-side work the reducer can't: `clearSelectionRequestsForPath`, `onSessionClosed` (disk-persisting analytics: finalize run as `'closed_unscored'` + dispatch `ActiveRunSummaryChanged(null)` — redundant since the reducer already cleared it, but idempotent), `clearSessionScope` (host-local runtime state + redundant-but-idempotent `SessionScopeCleared` dispatch), `evictInactiveTranscriptWindows`, `assertSelectionInvariant`, and the recursive `openSession(nextPath)` ONLY when nextPath is not null AND not summarized/pending (the edge case where a tab is open but its session hasn't been loaded — e.g. startup tab restore). `CloseSessionResult` moved to a dedicated no-op handler (out of the generic Log-on-!ok fallthrough), mirroring `CreateSessionResult`/`DuplicateSessionResult`. Hard-deleted: the fat `tabs.closeSession` body (`getNextVisibleTabPathOnClose` + `CloseTab` dispatch + `clearSessionScope`'s `SessionScopeCleared` + `saveOpenTabs` + `SelectSession` dispatches + placeholder creation), `closeSession` from the message-router's `SessionServiceLike` (unused — `onCloseSession` dispatches the Command directly), `removeSessionFromState` from `command-handlers.ts` imports (still used by `handleSessionClosed` in `session-handlers.ts`), `getNextVisibleTabPathOnClose` from `tab-actions.ts` imports (moved to the reducer). 13 TDD tests (10 reducer + 3 integration: next-tab-selection fix + e2e cleanup + recursive `openSession` edge case). Reviewer PASS (medium bucket, focused 10-risk task, one shot — no issues found). |
| `ac4fa86` | **Phase 2 — addFilesystemPaths (the LAST method-orchestration-lift).** Unlike create/open/duplicate/close (which have backend RPCs), addFilesystemPaths has NO backend RPC and NO side effect — it's purely a composer-input mutation. So the `AddFilesystemPathsEffect` + `AddFilesystemPathsResultEvent` are DELETED entirely (the simplest pattern, like `AddComposerInput` migrated in `487b430`). The reducer's handler was REWRITTEN from a pass-through to own the append: for each path, creates a `filesystemPathRef` `ComposerInput` (ID from `${corrId}:input:${index}`, name from `path.basename`), checks for duplicates against the running `nextInputs` array (catches both existing-input AND within-batch duplicates), skips empty paths, appends to `pendingComposerInputsBySession[sessionPath]`. `effects: []`. Added `import * as path from 'node:path'` (pure — not banned by the purity guards). The host-side entry (`message-actions.addFilesystemPaths`) was REWRITTEN as a thin entry: resolves the target session via `resolveComposerTargetSessionPath` (may call `createNewSession()` — **the entanglement the handoff flagged: a file-picker attach can spawn a new session**), cleans the paths (trim, filter empty, dedup), dispatches the `AddFilesystemPaths` Command with the RESOLVED session path (never `undefined`). Both entry points (`onOpenFilePicker` + `extension-host.attachFiles`) route through `service.addFilesystemPaths()` — the `attachFiles` bypass is fixed (it already called `service.addFilesystemPaths()`, which now dispatches the Command instead of doing the legacy imperative work). `AddFilesystemPathsCommand.sessionPath` changed from `string | undefined` to `string`. Deleted: the Effect type + union member, the Result event type + union member, the runner handler, `addFilesystemPaths` from the runner's `SessionServiceLike`, `AddFilesystemPathsResult` from the reducer switch + `handleEffectResult` fallthrough. 12 TDD tests (8 reducer + 3 integration + 1 updated). Reviewer PASS (medium bucket, focused 8-risk task, one shot — no issues found). |
| `2c18069` | **Phase 3 chunk 1 — pendingSendQueue.** Collapsed `QueueManager.pendingSendQueue` into `ArchState.pending.sendQueueBySession` (a `Record<string, PendingSendQueueEntry[]>`). The `Send` Command handler checks `isPendingTabPath(sessionPath)`: when true, queues the entry + inserts the optimistic message + clears the draft — but does NOT emit `SendRpc` or mark running or set `pending.ops`. `handlePendingPathReplaced` emits a `DrainPendingSendQueue` effect carrying the queued entries; the runner re-dispatches each as a `Send` Command with the resolved path (async void IIFE ensures they land AFTER the synchronous `SessionScopeCleared` + `SessionOpened` + `SelectSession` events — preserving the clear-then-reinsert ordering of the old `drainPendingSendQueue` callback). `previousSummary` stored as `null` in queue entries (by drain time the session has a real summary from `session.opened` — a non-null placeholder would revert the name on `SendResult{ok:false}`). `SessionScopeCleared` clears the queue (tab-close-before-resolve + createSession failure). `onSend` no longer short-circuits pending paths — all sends go through the `Send` Command; the reducer decides. Hard-deleted: `QueueManager.pendingSendQueue` + `enqueuePendingSend` + `drainPendingSendQueue`, `MessageRouter.drainPendingSendQueue` delegation, `OnSessionPathResolved` type + `onSessionPathResolved` from `SessionService` constructor + `SessionServiceEvents` + `attach.ts`, `isPendingTabPath` from `QueueManagerDeps`. New `dispatchCommand` callback on `EffectRunnerDeps`. 12 TDD tests (10 reducer + 2 runner). Reviewer PASS (medium bucket, 8-risk focused task, one shot). |
| `7605534` | **Phase 3 chunk 2 — backendReadyQueue + watchdog + QueueManager deletion.** Collapsed `QueueManager.backendReadyQueue` + the 30s watchdog into `ArchState.pending.backendReadyQueueBySession` + a runner-side timer. The `Send` Command handler checks `!state.settings.backendReady` (after `isPendingTabPath`): when true, queues + inserts optimistic + clears draft + emits `StartBackendReadyWatchdog` — no `SendRpc`, no running, no `pending.ops`. `handleBackendReadyChanged{ready:true}` emits `DrainBackendReadyQueue` + `CancelBackendReadyWatchdog`; the runner re-dispatches `Send` Commands + clears the timer. The watchdog timer lives in the runner (`backendReadyWatchdog` field); `StartBackendReadyWatchdog` starts it (no-ops if running), `BackendReadyWatchdogFired` → reducer drops messages via `removeMessage` + sets notice. `CancelBackendReadyWatchdog` on drain + `SessionScopeCleared` (only when the queue becomes empty). `dispose()` clears the timer on shutdown (replaces `clearBackendReadyQueueWatchdog`). `onSend` no longer short-circuits for `!backendReady`. `subscribeToArchState` no longer triggers drain (reducer-driven). **`QueueManager` class + `queue-manager.ts` FULLY DELETED** — the last non-ArchState state is gone. Also deleted dead code: `purgeHostStateForSession` (0 callers), `hasBackendReadyQueue` (0 callers), `QueueManager.dispose` (0 callers). New `dispatchEvent` callback on `EffectRunnerDeps`. 21 TDD tests (16 reducer + 3 runner + 2 E2E). Reviewer PASS (medium bucket, 10-risk focused task, one shot). |

### Established, reusable patterns (follow these for every remaining migration)
1. **Reducer owns the state change + emits a thin Effect; the runner executes only the side effect.** This is the send/edit model (already in place for send/edit/interrupt/moveSessionTab/setPrefs).
2. **Hard cutover per op:** once the reducer owns it, delete the legacy Effect + Result event + reducer/result-handlers cases + service method + interface decls in the same chunk. The exhaustiveness `never` checks are the safety net — a dangling reference fails the build.
3. **Optimistic apply + revert-on-failure** (send/edit-style) for settings that can fail: snapshot the pre-change value in `PendingState` keyed by `corrId`; on `*Result{ok:false}` revert + notice; on `{ok:true}` clear the snapshot. (Used for send/edit.) **Not universal** — `setPruningSettings` (commit `fca20c7`) deliberately does NOT snapshot/revert: the service keeps graceful-degradation (catch+mirror+notice, no throw) so the result is always `{ok:true}`, and the reducer's optimistic apply is authoritative. Choose revert-on-failure only when the side effect can actually fail in a way the user must be shielded from (and pair it with pattern #7 — verify the disk-write covers every field).
4. **Effects are thin descriptors**, not fat service calls. If an op needs a UI dialog, model it as a dedicated modal Effect with a result event the reducer branches on — never do a modal inside an effect's service call. **Established by setModel (`6b84d2a`):** `ShowModelSwitchConfirm` Effect + `ModelSwitchConfirmResult` — the reducer stashes the intent (`snapshot:null`) and emits the modal effect with NO state change; `{confirmed}` applies + emits the RPC, `{!confirmed}` drops the intent unchanged (so a Cancel leaves everything untouched). The modal is intentionally NOT queued (VS Code serializes modals; the backend write stays queued) so it doesn't block session create/open the way the old `enqueueLifecycle(modal)` path did. **Two supporting moves** future modal ops should reuse: (i) **relocate session guards into the reducer** (setModel moved `requireOpenSessionPath` in) so an invalid request can't leave an optimistic change un-reverted; (ii) **extract pure helpers** the reducer needs but that live in vscode-importing modules into a pure-spine module (setModel extracted `modelSupportsInputKind` to `core/model-capability.ts` so the reducer doesn't pull vscode into its test graph).
5. **Persist via `PersistTabs` Effect** (now wired) rather than imperative `saveOpenTabs()` calls — migrate each tab op to emit `PersistTabs` and drop its `saveOpenTabs()` call.
6. **Sub-agent rhythm that works:** scout (recon, precise file:line map) → worker (TIGHT, one well-bounded change — workers time out on multi-change design tasks) → reviewer (independent verification) → orchestrator commits only the extension/ files (never `settings.json`). For design-heavy chunks, the orchestrator does the design + core implementation directly and uses workers only for mechanical sub-tasks.
7. **Verify the disk-write covers every field when adding a reducer optimistic apply for a persisted setting.** The reducer's optimistic merge must match the disk-write merge field-for-field, else optimistic state ≠ persisted state (UI shows one value, disk/restart another — silent data loss). When lifting `setPruningSettings`, `writePruningSettings` had no `prepassTimeoutSec` handler (the field was read by `readPruningSettings` but never written), so the optimistic apply would have diverged from the disk; fixed in the same chunk. Grep the write handler for each persisted field before declaring optimistic==persisted.
8. **In-flight guard keyed by `corrId` (one-in-flight-per-session ops).** For ops that must serialize per session (transcript paging), the reducer owns a `Record<sessionPath, corrId>` flag: the Command handler blocks if the flag is set, else sets it = `cmd.corrId` + emits the Effect; the `*Result` clears it only when `event.corrId === flag[sp]` (a stale result from a superseded request — e.g. tab closed+reopened — must NOT clear the current flag); `SessionScopeCleared` clears it on tab close. This replaces a host-side `Set`/seq-counter pair: `corrId` is the request identity (consistent with send/edit `PendingOp`), so a separate monotonic seq counter is unnecessary. Because `dispatchArchEvent` is synchronous, the clear happens synchronously via the Result event (no timing regression vs an old `finally`). Used for transcriptPaging (`7ffec0d`); reusable for any future one-in-flight-per-session op.
9. **Host-local lifecycle bookkeeping that snapshots pre-reducer state must run BEFORE the reducer's optimistic change.** `createSession` (`9343b62`) keeps the selection token + 60s timer + `handleSelectionFailure` host-side in `SessionServiceState` (the reducer can't own a timer/counter), but `beginSelectionRequest` snapshots `previousActivePath = archState.sessions.activeSessionPath` for failure recovery. The reducer's `CreateSession` handler sets `activeSessionPath = pending` optimistically. So `beginSelectionRequest` MUST run before the Command dispatch (it does, in `service.createNewSession()`, before `dispatchArch`), else it snapshots the pending path and `handleSelectionFailure` restores the wrong tab on failure. The token then flows through Command→reducer→Effect→runner (the reducer passes it through unchanged; the runner uses it for the backend RPC). **Reusable for `openSession` (same `beginSelectionRequest` + `handleSelectionFailure` machinery): mint the token before the reducer activates the opened tab.** Pinned by `create-session-ordering.test.ts` + the e2e recovery test. When moving host-local bookkeeping that reads ArchState, audit what it snapshots and whether the reducer's optimistic change would corrupt the snapshot — if so, run the bookkeeping before the Command dispatch and flow the result through the spine.

### The safety net (keep it green; it catches real regressions)
- `extension/test/arch-boundary-guards.test.ts` — purity of the pure spine + import bans.
- The `never` exhaustiveness checks in `reducer.ts`, `effect-runner.ts`, `command-handlers.ts`, `result-handlers.ts`, `streaming-handlers.ts` — adding an Event/Effect/Command variant without a handler fails `tsc`.
- `npm run typecheck && npm run test && npm run build` after every `extension/src/` edit (per `AGENTS.md`). Suite is ~945 tests (~944 pass, 1 pre-existing skip). **Note:** there is unrelated working-tree dirt (`chat-prefs.test.ts` + `subagentAlwaysParentModel` field recently added to `ChatPrefs` by another commit — NOT from this migration; stash it or skip the build check until it's fixed separately).

---

## Remaining work (in suggested risk order; file:line are hints and may have shifted — locate by symbol)

### Phase 2 — COMPLETE ✅
All 13 method-orchestration-lifts are done. The MessageRouter is 100% Commands — zero SERVICE-DIRECT webview-message paths remain. See the table above for per-commit details. **Phase 3 is also COMPLETE — `QueueManager` is fully deleted. Move to Phase 4.**

### Phase 3 — COMPLETE ✅
`QueueManager` is fully deleted (`queue-manager.ts` removed). Both queues are collapsed into `ArchState.pending`:
- **`pendingSendQueue` → `sendQueueBySession`** (commit `2c18069`): the reducer queues when `isPendingTabPath`, `handlePendingPathReplaced` emits a `DrainPendingSendQueue` effect, the runner re-dispatches `Send` Commands with the resolved path (async IIFE ensures they land after the synchronous `SessionScopeCleared` + `SessionOpened` events — preserving the clear-then-reinsert ordering).
- **`backendReadyQueue` + watchdog → `backendReadyQueueBySession` + runner timer** (commit `7605534`): the reducer queues when `!backendReady`, `handleBackendReadyChanged{ready:true}` emits `DrainBackendReadyQueue` + `CancelBackendReadyWatchdog`, the runner re-dispatches `Send` Commands + clears the timer. The 30s watchdog timer lives in the runner (`backendReadyWatchdog` field); `StartBackendReadyWatchdog` starts it, `BackendReadyWatchdogFired` → reducer drops messages + sets notice. `SessionScopeCleared` clears both queues. `onSend` no longer short-circuits — all sends go through the `Send` Command; the reducer decides based on `isPendingTabPath` and `backendReady`. New `dispatchCommand` + `dispatchEvent` callbacks on `EffectRunnerDeps`.

**Move to Phase 4.**

### Phase 4 — COMPLETE ✅
Two chunks done + three architectural decisions recorded.

| Commit | Scope |
|--------|-------|
| `cd20851` | **Phase 4.1 — mechanical cleanup.** Replaced `subscribeToArchState` auto-projection (module-global listener Set in `dispatch.ts`) with an explicit `this.scheduleRender()` call at the end of `dispatchArchEvent` (after the effects loop). `dispatch.ts` is now a pure function — no module-level mutable state, no listener notification. Timing improvement: `scheduleRender()` now reads `this.archState` AFTER it's updated (the old auto-projection fired inside `dispatch()` before `this.archState = result.state`, so the render log showed stale state). Deleted dead code: `createComposerInputId`/`composerInputCounter` from `state.ts` (zero callers in session-service/; `composer.ts` uses its own inline callback in tests only); dead `runObserver` injection from `SessionMessageActions` (stored but never called) + from `service.ts` constructor + integration test fixtures. New `dispatch-purity.test.ts`. 8-risk reviewer PASS. |
| `7d78cc7` | **Phase 4.2 — `saveOpenTabs` removal.** Replaced the 2 `saveOpenTabs()` callers (`handleSelectionFailure` in `state.ts`, `finalizeSessionOpening` in `attach.ts`) with `PersistTabs` Command dispatches reading post-recovery/post-opening ArchState, routing through reducer → `PersistTabs` Effect → runner → `persistTabs` sink (the same sink used by all other tab ops since Phase 2). Deleted `saveOpenTabs()` from `state.ts` — it was a duplicate of the `PersistTabs` Effect handler in `extension-host.ts` (same storage keys, same JSON shape, same pending-path filtering). The previously-unused `PersistTabs` Command (defined in `commands.ts`, handled in `command-handlers.ts`) is now dispatched; its handler is a pass-through that emits the `PersistTabs` Effect; `PersistTabsResult` is a no-op. Single `PersistTabs` Command per call site (no double-persistence race). New `persist-tabs-via-command.test.ts`. 8-risk reviewer PASS. |

**Architectural decisions (recorded after scout recon + analysis):**
1. **`runObserver` calls — DECISION: stay as handler-side calls** (the doc's "stays as Effect-side calls" option). The 9 handler-side `runObserver` calls (in `streaming.ts`, `tools.ts`, `session.ts`, `attach.ts`) are in the backend event handler layer — the "backend adapter" that translates backend events into reducer Events + analytics. Routing them through the reducer → new Effects → runner would add ~100 lines of pass-through plumbing (9 new Effect types + runner handlers) with no architectural benefit: the reducer would just forward data it doesn't use (the observer is write-only analytics, not ArchState). The existing pattern is correct: observer calls associated with Command/Effect execution (e.g., `onModelConfigChanged` from `SetModelRpc`, `onSessionClosed` from `CloseSession`) are routed through the runner; observer calls associated with backend Event translation stay in the handler. The handlers still have genuinely host-local side-effects (`touchSessionTranscript`, `bindRequestSessionPath`, `acceptBusySeq`, `consumeCompletionSuppression`) that cannot move to the reducer regardless.
2. **`persistIfTabStateChanged` (boot-path) — DECISION: keep as-is.** It writes raw stored tab objects (with names from the previous session) to `globalState`, while the `PersistTabs` effect handler writes ArchState-derived `{path, name}` objects that need session summaries (not loaded yet at boot time — `SessionSummariesReplaced` dispatches later). Replacing would lose tab names from `globalState` until the next runtime tab op. It's a boot-time fixup, not a runtime orchestration path.
3. **Fat-handler migration (multi-step dispatches in `attach.ts`/`streaming.ts`/`tools.ts`/`session.ts`) — DECISION: deferred (not Phase 4).** These handlers compute flags from host-local selection requests + LRU + busy-seq state (e.g., `applySessionOpenedPayload` computes `shouldOpenTab`/`shouldActivate` from `SessionServiceState.selectionRequests`). Moving the multi-step dispatch logic into the reducer would require the reducer to know about host-local state. The handlers are the right place for this orchestration — they're the "backend adapter" that bridges backend events + host-local state into reducer Events. The `SessionService` methods themselves (`service.ts`, `tab-actions.ts`, `message-actions.ts`) are already thin entries (delegates to the reducer/runner).

### Phase 5 — COMPLETE ✅ (12 items done)

| Commit | Scope |
|--------|-------|
| `59c357f` | **Phase 5.1 — webview state-ownership leaks (D1, D5).** `clearTransientUi` now clears `pendingDraftRestoreRef.current` (Map of queued draft restores for non-active sessions — was never cleared, orphaned memory on session close) + calls new `clearDisclosureCache()` export from `use-disclosure-open.ts` (clears module-level `disclosureOpenByKey` + `disclosureDefaultByKey` Maps — were never cleared, stale open/closed state from previous sessions persisted across host-instance/session changes). 6-risk reviewer PASS. |
| `1b834d1` | **Phase 5.2 — fix `sendRejected` missing `text` (REAL BUG).** The reducer's `SendResult{ok:false}` handler emitted a `PostImperative` effect with `{ type: 'sendRejected', sessionPath, localId }` but WITHOUT the `text` field. The webview's `sendRejected` handler calls `draftOps.restoreNow(m.text)` — since `text` was missing, `m.text` was `undefined` and the user's draft was NOT restored on send failure. The loosely-typed `PostImperativeEffect.imperativeMessage` (`text?: string`) let TypeScript miss the omission. Fix: added optional `text?: string` to `PendingOp` (for send ops only); the `Send` Command handler stores `cmd.text` in `pending.ops[corrId].text`; the `SendResult{ok:false}` handler includes `text: pending.text ?? ''` in the `sendRejected` message. Edit ops unchanged. Queue drain flows preserve `text` end-to-end. 8-risk reviewer PASS. |
| `31ab778` | **Phase 5.3 — `canPostSnapshotToWebview` webviewReady gate.** Changed from `return hasView` (ignoring `webviewReady`) to `return hasView && webviewReady`. Previously, state was posted before the webview JS loaded — VS Code queued the postMessage, the `stateApplied` watchdog was armed, and on slow loads the 2.5s timeout fired before the webview could ack, causing force-reload storms. Now state is deferred until the webview signals readiness; `globalDirty` ensures the deferred state is posted when the webview's first message triggers `postState()`. 5-risk reviewer PASS. |
| `ff484df` | **Phase 5.4 — webview message listener guard.** Added a shape guard (`event.data` must be an object with a string `type` field) before `dispatchHostMessage` — defense-in-depth against malformed messages from browser extensions, devtools, or other non-host sources. The existing `dispatchHostMessage` already validates `type` against `HOST_MESSAGE_HANDLERS`; this prevents crashes from non-object `event.data`. `event.source`/origin verification intentionally omitted (VS Code webview host messages may have `event.source=null`). |
| `e835ab9` | **Phase 5.5 — recovery-by-reload re-snapshot-first.** On the first `stateApplied` timeout, re-post the state snapshot (`flushDirtyState` with `globalDirty=true`) instead of immediately force-reloading the webview HTML. The re-post arms a new watchdog via `postToWebview .then() → armStateAppliedWatchdog`. Only if the re-snapshot also goes unacked (second consecutive timeout, `resnapshotAttempted=true`) does the flow proceed to the existing throttle + force-reload path. `resnapshotAttempted` is reset on successful ack (`recordStateApplied`) and on webview-ready transition (fresh webview session). Avoids reload storms on slow transcripts. 7-risk reviewer PASS. |
| `dc4b16f` | **Phase 5.6 — transport recovery for `sendRejected`.** Two complementary fixes: (1) `reconcilePostedMessageDelivery` now marks `globalDirty` for `sendRejected` (was only `state`) — covers post attempted but delivery failed; (2) `postImperative` sets `globalDirty` for `sendRejected` when the webview isn't ready — covers message never posted. Both ensure the next `flushDirtyState` re-posts a state snapshot showing the error notice + transcript update. Fire-and-forget imperatives (`playCompletionSound`) remain silently dropped. The draft text is not restored on the lost-imperative path (it's not in `draftTextBySession`), but the user sees the error — strictly better than the pre-fix silent drop. 5-risk reviewer PASS. |
| `cee6916` | **Phase 5.7 — `pending.ops` + `requestIdToLocalId` cleanup in `handleSessionScopeCleared` + dead `extensionUiApproved` imperative removal.** `handleSessionScopeCleared` (tab close / session eviction) was NOT cleaning `pending.ops` or `pending.requestIdToLocalId` — orphaning entries if a `SendResult`/`EditResult` never arrived (backend crash, dropped event). Now filters both by `sessionPath`, mirroring `removeSessionFromState` in `helpers.ts`. This partially addresses the deferred optimistic-op TTL: the most common orphaning scenario (close tab mid-send) is now handled; the remaining gap (result never arrives + tab stays open) requires a runner-side watchdog + `Date.now()` in `PendingOp` (violates purity boundary) — still deferred (high risk, self-healing symptoms). Also removed the dead `extensionUiApproved` `PostImperative` emission from `RespondExtensionUI` — the webview had no handler for it; the state update (`pendingExtensionUIRequestsBySession` removal) already drives UI dismissal. 11 TDD tests (6 cleanup + 5 extension-ui-response). Reviewer PASS (7 risks, no issues). |
| `dff0594` | **Phase 5.8 — error-detail `dismissed` state (MVI fix).** The `ErrorDetail` component's local `dismissed` `useState` was an MVI violation when the error originated from the global `notice` (host-owned `ArchState.settings.notice`). The ✕ button now dispatches `dismissNotice` to the host (clearing the notice in ArchState via the `DismissNotice` Command) when the detail is from `notice`, and falls back to local `dismissed` state for per-message `errorDetail` (which has no host-side dismiss mechanism — the error is part of the transcript). `NoticeContext` extended from `string | null` to `{ notice: string | null; dismiss: (() => void) | null }` with a new `useDismissNotice()` hook; `app-body.tsx` provides `() => postMessage({ type: 'dismissNotice' })` as the dismiss callback. `NoticeBanner` is unaffected (uses direct props, not context). `useNotice()` return type is unchanged (backward-compatible). Reviewer PASS (8 risks, no issues). |
| `7070e79` | **Phase 5.9 — per-session keyed map cleanup parity.** `handleSessionScopeCleared` (tab close, common path) and `removeSessionFromState` (full eviction, rarer path) had diverged — each was missing 4 per-session keyed maps that the other cleaned, causing stale state on tab close → reopen cycles. `handleSessionScopeCleared` was missing: `editingMessageIdBySession` (stale edit state), `interruptInFlightBySession` (stale interrupt flag), `showOutcomeDialogBySession` (stale dialog), `currentTurnBySession` (stale turn). `removeSessionFromState` was missing: `pagingInFlightBySession` (stale paging guard), `sendQueueBySession` + `backendReadyQueueBySession` (orphaned queue entries), `setModelByCorrId` (orphaned lifecycle). Both now clean all 20 per-session keyed maps (excluding `draftTextBySession` which intentionally persists). Late `SetModelResult`/`ModelSwitchConfirmResult` gracefully no-op on cleaned entries (existing `!pending` guard). 11 TDD tests including an exhaustive parity test. Reviewer PASS (9 risks, no issues). |
| `74d84de` | **Phase 5.10 — dead `playCompletionSound` webview handler removal.** The `PlayCompletionSound` effect was deleted in Phase 0 (`a60f31c`) but the webview-side handler (`handlePlayCompletionSound` + `HOST_MESSAGE_HANDLERS` entry) + `HostToWebviewMessage` type member were not cleaned up. Removed the handler, import, type union member, and the now-inapplicable sidebar-sync test. The `playCompletionSound` function itself remains — it's called directly by the settings menu preview button (`settings-menu-subcomponents.tsx`). |
| `32a9983` | **Phase 5.11 — `PostImperativeEffect` type tightening + `messageIdAlias` memory leak fix + drain try/catch.** Three fixes: (1) `PostImperativeEffect.imperativeMessage` tightened from `{ type: string; ... }` to `PostImperativeMessage = Extract<HostToWebviewMessage, { type: 'sendRejected' }>` — the loose `type: string` let the Phase 5.2 `sendRejected` missing-`text` bug slip through; now a missing field is a compile error. `PostImperativeSink` tightened in tandem. (2) `messageIdAlias` changed from `Record<string, string>` to `Record<string, { canonicalId: string; sessionPath: string }>` — the old shape was keyed by message ID with no `sessionPath`, so neither `handleSessionScopeCleared` nor `removeSessionFromState` could clean it on session close (unbounded memory growth). Both cleanup functions now filter by `sessionPath`, matching the `requestIdToLocalId` pattern. (3) `DrainPendingSendQueue` + `DrainBackendReadyQueue` async IIFE bodies wrapped in try/catch with error logging. 5 TDD tests + 7 existing tests updated. Reviewer PASS (10 risks, no issues). |
| `cebce41` | **Phase 5.12 — optimistic-op TTL (the LAST deferred item).** Runner-side per-corrId watchdog for `SendRpc`/`EditRpc`. If the backend hangs and never responds to `message.send`/`session.truncateAfter`, the `pending.ops` entry was orphaned forever — the session appeared stuck busy and the optimistic message was never reverted. Follows the `BackendReadyWatchdog` pattern (Phase 3 chunk 2): runner starts a 60s timer inside `runSendRpc`/`runEditRpc` (after queue dequeue, right before `backend.request` — measures RPC duration, not queue wait); on success/failure, timer is cancelled before dispatching the `*Result`; on timeout, dispatches `SendResult`/`EditResult{ok:false, error:'Timed out'}` — reuses existing event types, no new effects/events/reducer changes. The reducer's existing `!pending` guard handles the race where a late real result arrives after the timeout (no-ops it). `dispose()` clears all timers on shutdown. `optimisticOpTimeoutMs` is configurable via `EffectRunnerDeps` (tests use 50ms; production default is 60s). 6 TDD tests (5 runner + 1 reducer late-arrival). Reviewer PASS (10 risks, no issues). |

**All deferred items resolved.** The optimistic-op TTL (Phase 5.12) was the last one.
- **Optimistic-op TTL:** ✅ DONE (Phase 5.12, `cebce41`). Runner-side per-corrId watchdog; no purity violation (`Date.now()` stays in the runner; the reducer only sees the existing `SendResult`/`EditResult{ok:false}` event).
- **Error-detail `dismissed` state:** ✅ DONE (Phase 5.8, `dff0594`).
- **Composer draft re-sync:** DECISION: skip. The `[sessionPath]`-only dep is intentional — adding `draftText` to the deps would introduce a data-loss race (a stale state snapshot could overwrite the user's current typing during the 300ms debounce + render delay). The `draftRestore` mechanism (with nonce) handles the one special case (`sendRejected`). There is no host-initiated `draftText` change for the same session outside of send/sendRejected in the current architecture.

---

## How to continue (concrete first steps for the next session)
1. `cd extension && npm run typecheck && npm run test` — confirm the foundation is green (expect ~1003 tests, ~1002 pass, 1 pre-existing skip). **Note:** `npm run build` may fail due to unrelated working-tree dirt (`chat-prefs.test.ts` — `subagentAlwaysParentModel` field recently added to `ChatPrefs` by another commit, test not updated). This is NOT from the MVI migration; stash the dirt or skip the build check until it's fixed separately.
2. **Phases 2 + 3 + 4 + 5 are COMPLETE.** All 13 method-orchestration-lifts are done + `QueueManager` is fully deleted + mechanical cleanup + `saveOpenTabs` removal done + 12 recovery/webview hardening chunks done (webview leak fixes, `sendRejected` text bug fix, `canPostSnapshotToWebview` gate, message listener guard, re-snapshot-first reload, `sendRejected` transport recovery, `pending.ops` cleanup on tab close + dead imperative removal, error-detail dismiss MVI fix, per-session keyed map cleanup parity, dead `playCompletionSound` handler removal, `PostImperativeEffect` type tightening + `messageIdAlias` memory leak fix + drain try/catch, optimistic-op TTL). The MessageRouter is 100% Commands. **The migration is complete — all deferred items resolved.** The orchestrator designs + implements the core directly, uses workers only for mechanical sub-tasks, and runs the reviewer gate.
3. Re-confirm current entry points with a grep before each migration (lines shift mid-migration).
4. Commit only `extension/` files; **never** `settings.json` (unrelated model-pref working-tree dirt). Doc updates to this handoff are a separate `docs/` commit (see git log: `ca041df`, `c36d6b4`, `34f4419`, `512c1e2`, `cbbc7b4`).
5. Every chunk: TDD (write/extend a reducer/integration test capturing behavior) → migrate → `never`-checks still compile → typecheck+test+build green → reviewer → commit. **Reviewer-gate operational note:** use the `medium` bucket (NOT `frontier` — it timed out twice at 600s) with a **focused, short task** (verify 7-10 specific risks, not the whole chunk). Every Phase 2 chunk from openSession onward PASSed in one shot with this approach. Two reusable lessons from the reviewer gate: (i) **when migrating a lifecycle/RPC op, add a params-asserting runner test** that locks the backend RPC payload field-for-field (e.g. `session.duplicate` carries the SOURCE path, not the pending copy path) — an `as any` service mock + a rejecting backend that ignores params will NOT catch a payload mixup (flagged by the duplicateSession reviewer, `882fe34`); (ii) **the reviewer catches real bugs** — for setModel (`6b84d2a`) it caught a data-loss regression (revert deleting unchanged file-ref inputs on the no-modal path); for closeSession (`d9712c7`) the orchestrator caught a latent double-execution bug in self-review (`removeSessionFromState` nulled `activeSessionPath` before the fat service could select the next tab). Keep the gate.

## Key file map (symbols to locate, not line numbers)
- Pure spine: `host/core/reducer.ts`, `arch-state.ts`, `effects.ts`, `events.ts`, `commands.ts`, `projection.ts`, `host/core/reducer/*` (command-handlers, result-handlers, streaming-handlers, host-handlers, session-handlers, misc-handlers, helpers, set-model-handlers, ui-handlers, optimistic-handlers).
- Impure plumbing in `core/` (NOT pure — excluded from purity guards): `host/core/message-router.ts`, **`queue-manager.ts` (Phase 3 target)**, `effect-runner.ts`, `dispatch.ts`, `event-dispatch.ts`, `file-diff-service.ts`, `composer.ts` (pure helpers + vscode-importing validation).
- Service layer (Phase 4 targets — now thin entries, not fat orchestration): `host/session-service/{service.ts, state.ts, tab-actions.ts, message-actions.ts, startup.ts, handlers/*, pruning-settings.ts, pruning-settings-persistence.ts}`. `tab-actions.ts` no longer imports `BackendClient` (all backend RPCs are in the runner). `SessionServiceState` (`state.ts`) still holds host-local bookkeeping: selection requests + 60s timer, epoch counters, transcript-window LRU, suppress flags, busy seq, `enqueueLifecycle` queue.
- `SessionServiceLike` interfaces: the runner's (`effect-runner.ts`) declares `handleSelectionFailure`, `closeSession(path, nextPath)`, `hydrateModelState`, `setPrefs`, `bumpSessionDataEpoch`, `onModelConfigChanged`, `suppressNextCompletionNotificationFor`, `loadOlder/Newer/jumpToLatestTranscript`, `setPruningSettings`. The router's (`message-router.ts`) declares `createNewSession`, `openSession`, `duplicateSession`, `addFilesystemPaths`, `setPrefs`, `setPruningSettings`, `loadOlder/Newer/jumpToLatestTranscript`.
- Transport: `host/sidebar/{sync.ts, provider.ts, completion-notification.ts}` (Phase 5 targets); webview: `webview/panel/hooks/use-host-sync.ts` + the components listed in Phase 5.
- Normalizer + guards: `host/util/error-message.ts`, `test/arch-boundary-guards.test.ts`.
- PersistTabs wiring + storage keys: `host/extension-host.ts` (`tabs.persistTabs`), `host/session-service/state.ts` (`OPEN_TABS_STORAGE_KEY`/`ACTIVE_SESSION_STORAGE_KEY`, `saveOpenTabs`).
- Test patterns to mirror: `test/arch-{create,open,duplicate,close}-session.test.ts` (reducer tests), `test/{create,open,duplicate,close}-session-ordering.test.ts` (ordering + e2e recovery), `test/arch-effect-runner.test.ts` (params-asserting runner tests for lifecycle RPCs), `test/arch-add-filesystem-paths.test.ts` (pure-reducer no-Effect pattern), `test/session-tab-actions.test.ts` (lifecycle serialization).